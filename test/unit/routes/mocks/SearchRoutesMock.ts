const graphRepositoryMockup = {
  findOne: jest.fn(),
};

const searchRoutesMockup = {
  search: jest.fn(),
};

const searchTownsMockup = {
  execute: jest.fn(),
};

export { graphRepositoryMockup, searchRoutesMockup, searchTownsMockup };
