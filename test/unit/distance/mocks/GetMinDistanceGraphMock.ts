const graphRepositoryMockup = {
  findOne: jest.fn(),
};

const searchMinDistanceMockup = {
  search: jest.fn(),
};

const searchTownsMockup = {
  execute: jest.fn(),
};

export { graphRepositoryMockup, searchMinDistanceMockup, searchTownsMockup };
